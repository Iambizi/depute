node_modules
dist
storybook-static
*.local
.DS_Store
storybook.log

# Internal docs (session notes, orchestration, research) — root only
/docs/

# Internal AI tooling (agent workflows, Claude-specific skill copies)
.agent/
.claude/
