---
title: Installation
description: Add Depute components to your project using the CLI.
---

import { Tab, Tabs } from 'fumadocs-ui/components/tabs';

## Requirements

- React 18+
- TypeScript
- CSS Modules support (standard in Vite, Next.js, CRA)

Depute is **agent-agnostic** — it works with any AI backend (OpenAI, Anthropic, LangChain, AutoGen, or custom).

## Install a component

<Tabs groupId="package-manager" items={["npm", "pnpm", "yarn", "bun"]} persist>
  <Tab value="npm">
```bash
npx ax-depute add approval-gate
```
  </Tab>
  <Tab value="pnpm">
```bash
pnpm dlx ax-depute add approval-gate
```
  </Tab>
  <Tab value="yarn">
```bash
yarn dlx ax-depute add approval-gate
```
  </Tab>
  <Tab value="bun">
```bash
bunx ax-depute add approval-gate
```
  </Tab>
</Tabs>

This copies the component source files directly into `src/components/ApprovalGate/`. You own the code.

On first install, shared files are also added once:

- `src/types/ax-common.ts` — shared TypeScript types (`PlanStep`, `ToolCall`, `Artifact`)
- `src/utils/ax-a11y.tsx` — accessibility utilities used across components

## List all components

<Tabs groupId="package-manager" items={["npm", "pnpm", "yarn", "bun"]} persist>
  <Tab value="npm">
```bash
npx ax-depute list
```
  </Tab>
  <Tab value="pnpm">
```bash
pnpm dlx ax-depute list
```
  </Tab>
  <Tab value="yarn">
```bash
yarn dlx ax-depute list
```
  </Tab>
  <Tab value="bun">
```bash
bunx ax-depute list
```
  </Tab>
</Tabs>

## Install multiple components

<Tabs groupId="package-manager" items={["npm", "pnpm", "yarn", "bun"]} persist>
  <Tab value="npm">
```bash
npx ax-depute add plan-card
npx ax-depute add tool-trace
npx ax-depute add run-controls
```
  </Tab>
  <Tab value="pnpm">
```bash
pnpm dlx ax-depute add plan-card
pnpm dlx ax-depute add tool-trace
pnpm dlx ax-depute add run-controls
```
  </Tab>
  <Tab value="yarn">
```bash
yarn dlx ax-depute add plan-card
yarn dlx ax-depute add tool-trace
yarn dlx ax-depute add run-controls
```
  </Tab>
  <Tab value="bun">
```bash
bunx ax-depute add plan-card
bunx ax-depute add tool-trace
bunx ax-depute add run-controls
```
  </Tab>
</Tabs>

## Shared types

All components use these core types from `src/types/ax-common.ts`:

```ts
interface PlanStep {
  id: string;
  label: string;
  status: 'pending' | 'active' | 'completed' | 'failed';
  confidence?: number;
}

interface ToolCall {
  id: string;
  name: string;
  input: unknown;
  output?: unknown;
  status: 'running' | 'completed' | 'failed';
  policyFlags?: ('requiresApproval' | 'writesState' | 'externalAction')[];
}

interface Artifact {
  id: string;
  content: string;
  format: 'markdown' | 'json' | 'csv' | 'code';
  sourceStepId?: string;
  toolCallIds?: string[];
}
```

## No backend required

Use the built-in mock data generators for prototyping:

```tsx
import { generateMockPlan, generateMockToolCalls } from '@/utils/mockData';

const plan = generateMockPlan(4);       // 4-step agent plan
const trace = generateMockToolCalls(3); // 3 tool call entries
```

The mock generators show the exact shape each component expects.
